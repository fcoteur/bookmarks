extends layout

block content
  h1= title
  p Welcome to #[em Bookmarks], a very basic website developped with Node.js, Express and Mongo database.

  h2 Content of this site

  if error
    p Error getting dynamic content.
  else
    ul
      li #[strong Bookmarks :] !{data.bookmark_count}
      li #[strong Groups :] !{data.group_count}
    
    br
    a(href='/bookmarks/bookmarks') Go to list of bookmarks
    br
    a(href='/bookmarks/groups') Go to list of groups

  h2 Login to edit content

  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/logout") Logout